# AI-Based FYP Relevancy System – Backend Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# PostgreSQL + ORM (async)
sqlalchemy[asyncio]==2.0.36
greenlet>=3.0.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Environment variables (.env)
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Authentication (JWT + bcrypt password hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# HTTP & file uploads
python-multipart==0.0.17
aiofiles==24.1.0
httpx==0.28.1

# AI relevancy (cosine similarity)
numpy==2.2.1

# AI relevancy V2 prerequisites (semantic embeddings — not wired to scoring yet)
# Python 3.12 compatible. On Windows CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0,<3
sentence-transformers>=3.3.0,<6

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
